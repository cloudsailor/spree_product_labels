sk:
  spree:
    notice_messages:
      label:
        product_deleted: Product has been deleted from the label '%{label_name}'
    admin:
      tab:
        labels: Labels
        label: Label
      labels: Labels
      label:
        name: Name
        description: Description
        label_type: Label Type
        position: Position
        start_date: Start Date
        end_date: End Date
        always_active: Always Active
        status: Status
        color: Color
        custom_label_type: ← Select "Other" Label Type if you want to create a new one
        other_type: Other
        products: Label products
        products_count: Products Count
        new_label: New Label
        edit_label: Edit Label
        remove_product: Remove from '%{label_name}'
        validates:
          date_blank: Start date and end date must be present unless the label is always active
          end_date_after_start_date: must be after start date
          only_one_active_label_per_priority: "Only one active label with position %{position} can exist at a time"
          only_one_active_label_per_type: Only one active label of a given type is allowed
          no_overlapping_validity_dates: The validity dates of a given label type cannot overlap
          color_format: 'Must be a valid color format (e.g., #FF0000)'
        created: Label Created
        updated: Label Updated
        deleted: Label Deleted
        not_found: Label not found
        not_found_in_current_store: Label not found in current store
  flash:
    actions:
      create:
        alert: Label could not be created
      update:
        alert: Label could not be updated
      import:
        file_not_found: No file was uploaded
        success: Product import has been started in the background


