# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in spree-spree_product_labels.gemspec.
group :development, :test do
  gem 'brakeman'
  gem 'rubocop'
  gem 'rubocop-rails'
end

gemspec
